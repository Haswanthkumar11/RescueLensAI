VITE_API_URL=http://localhost:8000

# Supabase project settings -> API. The anon/public key is safe to
# expose in frontend code (it's rate-limited and RLS-restricted) -- do
# NOT put the service-role key here.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-public-key-here
